<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for call screening and monitoring -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Strip unused foreground-service permissions merged in by the Linphone SDK
         (no video calls, and our call services declare their own FGS types) —
         Play Console requires a declaration for every FGS permission in the bundle -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" tools:node="remove" />
    <!-- WorkManager merges this for expedited jobs; we schedule none (no setExpedited
         anywhere), so its dataSync foreground service can never start -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" tools:node="remove" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Optional: shows an on-screen warning overlay during detected scam calls -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Family Guard Mode: sends SMS alerts to caregivers when a scam is detected -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- Family Guard Mode: makes an outgoing call to the primary caregiver after a scam call ends -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- Read device contacts to skip scam analysis for known callers/senders -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Telephony hardware is optional — app runs on tablets/ChromeOS without a SIM -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:name=".VocaGuardApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VocaGuard">
        <activity
            android:name=".ui.IncomingCallActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.VocaGuard" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VocaGuard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep-link: vocaguard://alert?name=…&type=…&conf=…&ts=… -->
            <!-- Caregivers tap this link in the SMS to import the alert into their dashboard. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vocaguard" android:host="alert" />
            </intent-filter>
        </activity>

        <!-- Call Screening Service for pre-call screening -->
        <service
            android:name=".service.ScamCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Notification Listener Service for WhatsApp / Telegram message scanning -->
        <service
            android:name=".service.MessagingScamDetectorService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Foreground Service for call monitoring -->
        <service
            android:name=".service.CallMonitoringService"
            android:foregroundServiceType="microphone"
            android:enabled="true"
            android:exported="false" />

        <!-- Persistent foreground service: TelephonyCallback-based call detection
             (fallback for Samsung devices where CallScreeningService is not invoked) -->
        <service
            android:name=".service.PhoneMonitorService"
            android:foregroundServiceType="specialUse|microphone"
            android:enabled="true"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent call monitoring for real-time scam detection" />
        </service>

        <!-- Boot receiver to start monitoring on device boot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Phone state receiver — Samsung freezes TelephonyCallback in background,
             but manifest-registered broadcast receivers are always delivered -->
        <receiver
            android:name=".receiver.PhoneStateReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Block-caller broadcast receiver (notification action) -->
        <receiver
            android:name=".receiver.BlockCallerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.vocaguard.ACTION_BLOCK_CALLER" />
            </intent-filter>
        </receiver>

        <!-- Active call hang-up button in persistent notification -->
        <receiver
            android:name=".receiver.ActiveCallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.vocaguard.ACTION_HANG_UP" />
            </intent-filter>
        </receiver>

        <!-- Home screen widget -->
        <receiver
            android:name=".widget.VocaGuardWidget"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- FCM service — receives scam alerts from Asterisk server -->
        <service
            android:name=".service.VocaGuardFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>